all:
  hosts:
    web:
      ansible_host: 44.201.191.44
      ansible_port: 22
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ansible_server.pem

#ansible web -i aws.yaml -m ping
#ansible web -i aws.yaml -m ping